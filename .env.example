# git-issue-sync configuration
# Copy this file to .env and update with your settings

# Required: GitHub repository to sync (owner/repo format)
GITHUB_REPO=owner/repo

# Output Configuration (defaults shown)
OUTPUT_DIR=issues
IMAGES_SUBDIR=images

# Sync Behavior
# SYNC_CLOSED: How to handle closed issues
#   false (default) - Only sync open issues. Delete local files when issues close.
#   true - Sync all issues (open + closed). Keep closed issue files.
SYNC_CLOSED=false

# Image Handling
IMAGE_RETRIES=3

# Logging
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
