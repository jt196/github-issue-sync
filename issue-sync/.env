# github-issue-sync configuration
# Copy this file to .env and update with your settings

# Required: GitHub repository to sync (owner/repo format)
GITHUB_REPO=owner/repo

# Output Configuration (defaults shown)
# Advanced: If you change OUTPUT_DIR, run the first sync with
# OUTPUT_DIR=/absolute/path so the template folder seeds correctly.
OUTPUT_DIR=.github
IMAGES_SUBDIR=images

# Sync Behavior
# SYNC_CLOSED: How to handle closed issues
#   false (default) - Only sync open issues. Delete local files when issues close.
#   true - Sync all issues (open + closed). Keep closed issue files.
SYNC_CLOSED=false

# Image Handling
IMAGE_RETRIES=3

# Logging
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Agent Behavior (for AI coding assistants)
# PLANS_CREATE: When agents should create implementation plans
#   ask (default) - Agent asks user if a plan is needed
#   always - Always create a plan before implementing
#   never - Skip plans, implement directly
PLANS_CREATE=ask
